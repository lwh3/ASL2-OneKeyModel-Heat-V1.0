<UserControl x:Class="HeatModelMonitor.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- 统计卡片 -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:Card Grid.Column="0" Margin="0,0,10,0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="总任务数" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding TotalTasks}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="1" Margin="5,0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="待处理" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding PendingTasks}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0" Foreground="Orange"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="2" Margin="5,0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="已完成" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding CompletedTasks}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0" Foreground="Green"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="3" Margin="10,0,0,0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="错误任务" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding ErrorTasks}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0" Foreground="Red"/>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>

            <!-- 最近任务 -->
            <materialDesign:Card Margin="0,20,0,0">
                <StackPanel>
                    <Border Background="{DynamicResource PrimaryHueMidBrush}" Padding="15">
                        <Grid>
                            <TextBlock Text="最近任务" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                            <Button Content="刷新" Style="{StaticResource MaterialDesignFlatButton}"
                                   Foreground="White" HorizontalAlignment="Right"
                                   Command="{Binding RefreshCommand}"/>
                        </Grid>
                    </Border>

                    <DataGrid ItemsSource="{Binding RecentTasks}"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             Margin="10"
                             CanUserSortColumns="True"
                             materialDesign:DataGridAssist.CellPadding="10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="任务ID" Binding="{Binding TaskId}" Width="150"/>
                            <DataGridTextColumn Header="钢种" Binding="{Binding SteelGrade}" Width="100"/>
                            <DataGridTextColumn Header="阶段" Binding="{Binding HeatStage}" Width="100"/>
                            <DataGridTextColumn Header="当前温度" Binding="{Binding CurrentTemp, StringFormat={}{0}°C}" Width="100"/>
                            <DataGridTextColumn Header="目标温度" Binding="{Binding TargetTemp, StringFormat={}{0}°C}" Width="100"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="80"/>
                            <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>

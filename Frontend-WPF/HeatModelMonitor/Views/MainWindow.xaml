<Window x:Class="HeatModelMonitor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="LF精炼炉加热模型监控系统" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 侧边栏 -->
        <Border Grid.Column="0" Background="{DynamicResource PrimaryHueDarkBrush}">
            <DockPanel>
                <!-- Logo -->
                <Border DockPanel.Dock="Top" Height="80" BorderBrush="{DynamicResource PrimaryHueDarkBrush}" BorderThickness="0,0,0,1">
                    <TextBlock Text="LF加热模型" FontSize="20" FontWeight="Bold"
                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- 菜单 -->
                <ListBox ItemsSource="{Binding MenuItems}"
                         SelectedItem="{Binding SelectedMenuItem}"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="White"
                         Margin="0,10,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Height="50">
                                <materialDesign:PackIcon Kind="{Binding Icon}"
                                                        Width="24" Height="24"
                                                        VerticalAlignment="Center" Margin="20,0,15,0"/>
                                <TextBlock Text="{Binding Title}" FontSize="16" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- 主内容区 -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 顶部栏 -->
            <Border Grid.Row="0" Background="White" Height="60"
                    BorderBrush="#E0E0E0" BorderThickness="0,0,0,1"
                    Effect="{DynamicResource MaterialDesignShadowDepth1}">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{Binding CurrentTitle}" FontSize="24" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:Chip Content="{Binding ConnectionStatus}"
                                            Margin="0,0,20,0"
                                            Background="{Binding ConnectionColor}"/>
                        <TextBlock Text="{Binding CurrentTime}" FontSize="14"
                                  Foreground="#666" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 内容区域 -->
            <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ContentRegion"
                           Margin="20"/>
        </Grid>
    </Grid>
</Window>
